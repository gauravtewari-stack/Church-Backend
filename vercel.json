{
  "buildCommand": "cd admin-ui && npm install && npm run build",
  "outputDirectory": "admin-ui/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
